//Create a new component and this component should produce some HTML

//Take this component's generated HTML and put it on the page (in DOM)

const App = function () {
    //JSX : HTML looking stuff, Subset or dialect of JS.
    return <div>Hi!</div>;
}